package metrics

/*
// Tests that the RegisterRoutes successfully registers the provided Endpoint function at the /metrics path.
func TestRegisterRoute(t *testing.T) {
	router := mux.NewRouter()
	RegisterRoutes(router)
	route := router.GetRoute("metrics")
	assert.Equal(t, "metrics", route.GetName())
}
*/
